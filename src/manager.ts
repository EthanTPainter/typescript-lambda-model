/**
 * Manager class allows for dependency injection with clients/repository layers passed
 * into the Manager constructor.
 *
 * Necessary paramters (IDs, arrays, Objects, etc.) can be passed in directly to a
 * manager function
 */
export class Manager {
	/**
	 * Any clients/repository layers defined in controller constructor
	 */
	constructor() {}

	/**
	 * Example function within Manager class to process business logic
	 */
	process() {}
}
